<%= form_for @target do |f| %>
  <% if @target.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@target.errors.count, "error") %> prohibited this target from being saved:</h2>

      <ul>
      <% @target.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_field :notes %>
  </div>

  <div class="field">
    <%= f.label :flavor %>
    <% if params[:flavor_id].present? %>
      <%= f.collection_select :flavor_id, @flavors, :id, :name, prompt: true, include_blank: true, selected: params[:flavor_id] %>
    <% else %>
      <%= f.collection_select :flavor_id, @flavors, :id, :name, prompt: true, include_blank: true  %>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
